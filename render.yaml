services:
  - type: web
    name: atg-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --timeout 30 --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
